name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Content is up to date with docs-source
        # prettier mirrors the local pre-commit hook, which reformats the
        # generated JSON — without it the diff fails on formatting alone
        run: |
          npm run generate-content
          npx prettier --write content/
          git diff --exit-code content/ || {
            echo "content/*.json diverge de docs-source/ — lancez npm run generate-content et committez."
            exit 1
          }

      - name: Type-check
        run: npm run type-check

      - name: Lint
        run: npx eslint . --ext .ts,.tsx --max-warnings 0

      - name: Tests
        run: npm test -- --ci
